export default function NoPhotosIcon({ className = 'text-[14px]' }) {
  return (
    <span
      className={`material-symbols-outlined flex-shrink-0 text-ink-400 ${className}`}
      title="No photos"
      aria-label="No photos"
    >
      no_photography
    </span>
  )
}
