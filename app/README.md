# BarnDoors — App

React + Vite + Tailwind frontend for BarnDoors (see repo root `AGENTS.md` for full
project context and locked-in tech stack decisions).

## Getting started

```bash
npm install
npm run dev
```

## Current status

Early wireframe stage — just the home screen shell (top nav + placeholder section
buttons). No routing, Supabase connection, or auth wired up yet.
